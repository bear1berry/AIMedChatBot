# bot/config.py
import os
from pathlib import Path

from dotenv import load_dotenv

# Базовые пути и .env
BASE_DIR = Path(__file__).resolve().parent.parent
ENV_PATH = BASE_DIR / ".env"

if ENV_PATH.exists():
    load_dotenv(ENV_PATH)
else:
    load_dotenv()

# Токены и ключи
BOT_TOKEN = os.getenv("BOT_TOKEN")
DEEPSEEK_API_KEY = os.getenv("DEEPSEEK_API_KEY")
DEEPSEEK_MODEL = os.getenv("DEEPSEEK_MODEL", "deepseek-chat")
DEEPSEEK_BASE_URL = os.getenv("DEEPSEEK_BASE_URL", "https://api.deepseek.com")

if not BOT_TOKEN:
    raise RuntimeError("BOT_TOKEN is not set in environment variables")

if not DEEPSEEK_API_KEY:
    raise RuntimeError("DEEPSEEK_API_KEY is not set in environment variables")

# Админские ID (через запятую в .env: ADMIN_USER_IDS=12345,67890)
_admin_raw = os.getenv("ADMIN_USER_IDS", "").replace(" ", "")
ADMIN_USER_IDS = {int(x) for x in _admin_raw.split(",") if x.isdigit()}

# Моды ассистента
DEFAULT_MODE_KEY = "universal"

ASSISTANT_MODES = {
    "universal": {
        "title": " 🧠 Универсальный",
        "description": "Ответы на любые вопросы: от жизни до кода.",
        "system_prompt": (
            "Ты — универсальный ИИ-ассистент BlackBoxGPT. "
            "Отвечай понятно, структурированно и по делу. "
            "Если вопрос про медицину, напоминай, что это не замена очной консультации."
        ),
    },
    "med": {
        "title": "⚕️ Медицина",
        "description": "Разбор симптомов, анализов, статей и исследований.",
        "system_prompt": (
            "Ты — медицинский ассистент. Объясняй исследования, симптомы, анализы. "
            "Не ставь диагнозы и не давай окончательных схем лечения. "
            "Всегда добавляй, что это не замена очной консультации врача."
        ),
    },
    "coach": {
        "title": " 🧩 Наставник",
        "description": "Режим, дисциплина, мотивация и разбор целей.",
        "system_prompt": (
            "Ты — наставник и коуч. Помогаешь выстраивать режим, цели, привычки. "
            "Говори честно, но поддерживающе. Предлагай конкретные шаги."
        ),
    },
    "biz": {
        "title": " 💼 Бизнес / Идеи",
        "description": "Стартапы, продукты, стратегии и анализ ниш.",
        "system_prompt": (
            "Ты — стратег по бизнесу и продуктам. "
            "Помогаешь с идеями, моделями монетизации, структурой проектов. "
            "Давай чёткие, реалистичные шаги."
        ),
    },
    "creative": {
        "title": " 🎨 Креатив",
        "description": "Тексты, идеи, концепции, названия, визуальные концепты.",
        "system_prompt": (
            "Ты — креативный партнёр. Генерируешь названия, идеи, концепции, тексты. "
            "Можешь предлагать необычные решения, но всегда учитывай запрос пользователя."
        ),
    },
}

# Тарифы и лимиты
# В интерфейсе теперь только два тарифа:
#  - "Базовый" (free) — по умолчанию
#  - "Premium" (premium) — платный
PLAN_LIMITS = {
    "free": {
        "title": "Базовый",
        "daily_base": 50,
        "description": "Базовый бесплатный доступ для повседневных задач.",
    },
    "premium": {
        "title": "Premium",
        "daily_base": 500,
        "description": "Для тех, кто использует ассистента как рабочий инструмент.",
    },
}

# Бонус за одного приглашённого пользователя (суточный лимит)
REF_BONUS_PER_USER = 5

# Ссылки оплаты в USDT через CryptoBot (указываешь в .env)
# CRYPTO_USDT_LINK_MONTH=https://t.me/CryptoBot?start=XXXX
# CRYPTO_USDT_LINK_3M=https://t.me/CryptoBot?start=YYYY
# CRYPTO_USDT_LINK_YEAR=https://t.me/CryptoBot?start=ZZZZ
CRYPTO_USDT_LINK_MONTH = os.getenv("CRYPTO_USDT_LINK_MONTH", "")
CRYPTO_USDT_LINK_3M = os.getenv("CRYPTO_USDT_LINK_3M", "")
CRYPTO_USDT_LINK_YEAR = os.getenv("CRYPTO_USDT_LINK_YEAR", "")

# Сколько сообщений истории хранить на пользователя
MAX_HISTORY_MESSAGES = 20
